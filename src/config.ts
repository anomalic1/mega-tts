/**
 * Deployment behavior switches.
 *
 * REQUIRE_SIGN_IN — when true, the studio is gated behind Google sign-in
 * (visitors are redirected to /signin). Flip to false to re-enable open
 * guest access.
 */
export const REQUIRE_SIGN_IN = true
